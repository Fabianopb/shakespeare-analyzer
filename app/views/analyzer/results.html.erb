<h1>Analyzer#results</h1>
<p>Find me in app/views/analyzer/results.html.erb</p>

<ul>
  <% @speechCount.each do |speaker, lines| %>
    <li><%= "#{speaker}, #{lines}" %></li>
  <% end %>
</ul>

<%= link_to 'Data', action: 'results_data' %>
